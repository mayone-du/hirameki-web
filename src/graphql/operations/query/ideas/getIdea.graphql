query GetIdea($id: ID!) {
  idea(id: $id) {
    id
    title
    content
    createdAt
    ideaCreator {
      id
      relatedUser {
        id
        profileName
        profileImage
        googleImageUrl
      }
    }
  }
}
