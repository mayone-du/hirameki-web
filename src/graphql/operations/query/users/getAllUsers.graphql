query GetAllUsers {
  allUsers(isSuperuser: false, isActive: true) {
    edges {
      node {
        id
        email
        username
        relatedUser {
          id
          profileName
          selfIntroduction
        }
      }
    }
  }
}
