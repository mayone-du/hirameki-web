query GetUserSettings($id: ID!) {
  user(id: $id) {
    id
    relatedUser {
      id
      profileName
      profileImage
      googleImageUrl
      selfIntroduction
      githubUsername
      twitterUsername
      websiteUrl
    }
  }
}
